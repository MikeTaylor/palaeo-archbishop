define(`NAME', `equalweights-c1')dnl
define(`MAYBE_PIWE', `dnl')dnl
define(`MAYBE_XPIWE', `dnl')dnl
define(`CONSTRAINTS', `force / (Shunosaurus (Saltasaurus (Brachiosaurus Archbishop)));;
constrain =;
')dnl
include(template.m4)dnl
